// This component will be loaded by next until the page is fully loaded
// Also check <suspense> component to use loading components

export default function loadingComponent() {
  return <div>...loading</div>;
}
